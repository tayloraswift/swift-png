matrix:
  include:
  - os: linux
    language: generic
    sudo: required
    dist: trusty
    env: 
    - SWIFT_BRANCH=swift-4.2-branch SWIFT_VERSION=swift-4.2-DEVELOPMENT-SNAPSHOT-2018-08-18-a
    install:
    - mkdir swift
    - curl https://swift.org/builds/$SWIFT_BRANCH/ubuntu1404/$SWIFT_VERSION/$SWIFT_VERSION-ubuntu14.04.tar.gz -s | tar xz -C swift &> /dev/null
    script:
    - env PATH=$(pwd)/swift/$SWIFT_VERSION-ubuntu14.04/usr/bin:$PATH swift build 
    - .build/debug/tests
